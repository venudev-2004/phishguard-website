SECRET_KEY=change-me-in-production

# ── Database ──────────────────────────────────────────────────────────────────
# DATABASE_URL=sqlite:///phishing_detector.db
# For PostgreSQL:
# DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/phishing_detector

# ── Optional custom folders ───────────────────────────────────────────────────
# UPLOAD_FOLDER=/absolute/path/to/uploads
# LOG_FOLDER=/absolute/path/to/logs

# ── Flask environment ─────────────────────────────────────────────────────────
FLASK_ENV=development

# ═════════════════════════════════════════════════════════════════════════════
# EXTERNAL SECURITY API KEYS
# Any key left blank → that check is silently skipped (no crash).
# ═════════════════════════════════════════════════════════════════════════════

# ── Google Safe Browsing API v4 ───────────────────────────────────────────────
# 1. Go to https://developers.google.com/safe-browsing/v4/get-started
# 2. Enable "Safe Browsing API" in Google Cloud Console
# 3. Create an API key under Credentials → API Key
# Free quota: 10,000 queries/day
GOOGLE_SAFE_BROWSING_API_KEY=

# ── VirusTotal API v3 ─────────────────────────────────────────────────────────
# 1. Register at https://www.virustotal.com/
# 2. Go to your profile → API Key
# Free tier: 4 requests/minute, 500 requests/day
VIRUSTOTAL_API_KEY=

# ── URLScan.io (optional – enables screenshot capture) ────────────────────────
# 1. Register at https://urlscan.io/
# 2. Go to Settings → API Keys → Create
# Free tier: 5,000 unlisted scans/day
# Enable by setting both the key AND ENABLE_URLSCAN=1
URLSCAN_API_KEY=
ENABLE_URLSCAN=0

# ── API behaviour ─────────────────────────────────────────────────────────────
# Timeout in seconds for every external HTTP call (default 10)
# API_TIMEOUT=10

# Seconds between VirusTotal calls to respect free-tier rate limit (default 15)
# VT_RATE_LIMIT_DELAY=15

# How long (hours) to cache external API results locally (default 6)
# API_CACHE_TTL_HOURS=6
